<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">
 	<select id="selectOneMember" parameterType="m" resultType="m">
 		select 
 			member_no as memberNo,
 			member_id as memberId,
 			member_password as memberPassword,
 			member_level as memberLevel
 		from member where member_id=#{memberId} and member_password=#{memberPassword}
 	</select>
 	<insert id="insertMember" parameterType="m">
 		insert into member values(member_seq.nextval, 
								#{memberId},#{memberEmail},
								#{memberPassword},#{memberName},
								#{memberPhone},#{memberBirthday},#{memberGender},
								#{postcode},#{addressRoad},#{addressDetail},
								1,#{memberReceiveEmail},#{memberReceiveSms},1,to_char(sysdate,'yyyy-mm-dd'))
 	</insert>
 	<select id="selectOne" parameterType="string" resultType="m">
 		select 
 			member_id as memberId
 		from member where member_id=#{memberId}
 	</select>
</mapper>
